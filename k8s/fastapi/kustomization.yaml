apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

configMapGenerator:
  - name: fastapi-config
    envs:
      - .env.config

secretGenerator:
  - name: fastapi-secret
    envs:
      - .env.secret

resources:
  - service-fastapi.yaml
  - deployment-fastapi.yaml
