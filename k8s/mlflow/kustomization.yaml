apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

images:
  - name: image-mlflow
    newName: accidents_severity/mlflow
    newTag: "1.0"

resources:
  - service.yaml
  - pvc.yaml
  - deployment.yaml
