# Dans kubernetes, pas de logique (if, else), pas de variables dynamiques ${VAR}
# pas de calcul, pas de templating runtime (sauf outils externes), pas de “shell execution”
# Donc une variable doit avoir une affectation fixe
# Bonne pratique : format KEY=VALUE.
# Les guillemets sont inutiles sauf si espaces ou caractères spéciaux.

# --- Global ---
PROJECT_NAME=accidents_severity
# Ces URI servent aux appels internes au cluster : le runner qui enregistre un run, l'API qui charge un modèle.
# Ils passent par le DNS de Kubernetes (mlflow → ClusterIP) sans jamais toucher l'Ingress.
# Et le Service mlflow déclare port: 80 → targetPort: 5000. Le port: est le seul point d'entrée exposé par la ClusterIP;
# Le 5000 n'est connu que de kube-proxy pour la redirection interne.
# Viser mlflow:5000 tape sur un port où la ClusterIP n'écoute pas — même piège que l'initContainer FastAPI et le nc -z evidently-ui 8000.
#DOCKER_MLFLOW_TRACKING_URI=http://mlflow:5000
DOCKER_MLFLOW_TRACKING_URI=http://mlflow:80

# --- Mlflow ---
MLFLOW_EXPERIMENT_NAME=accidents_severity
#MLFLOW_TRACKING_URI=http://mlflow:5000
MLFLOW_TRACKING_URI=http://mlflow:80

# --- Airflow core ---
AIRFLOW__CORE__EXECUTOR=CeleryExecutor
AIRFLOW__CORE__DAGS_ARE_PAUSED_AT_CREATION=true
AIRFLOW__CORE__LOAD_EXAMPLES=false
#AIRFLOW__CORE__BASE_LOG_FOLDER=/opt/airflow/logs
# Sert à dire comment déterminer le hostname (variable HOSTNAME)
#AIRFLOW__CORE__HOSTNAME_CALLABLE=socket.gethostname
# socket.gethostname() retourne le nom du pod (airflow-nnn-xx), que le DNS Kubernetes ne sait pas résoudre — seuls les Services le sont.
# Avec get_host_ip_address, le worker s'enregistre avec son IP (10.42.0.x), qui est routable dans le cluster.
AIRFLOW__CORE__HOSTNAME_CALLABLE=airflow.utils.net.get_host_ip_address
# GIT_SYNC_DEST = mlops_accidents_k3s ce qui permet d'avoir la même arborescence que sur github
AIRFLOW__CORE__DAGS_FOLDER=/git/mlops_accidents_k3s/deployments/airflow/dags
AIRFLOW__CORE__PLUGINS_FOLDER=/git/mlops_accidents_k3s/deployments/airflow/plugins
# Relecture des dags toutes les 30s
AIRFLOW__SCHEDULER__DAG_DIR_LIST_INTERVAL=30

# --- Database ---
AIRFLOW__DATABASE__SQL_ALCHEMY_CONN=postgresql+psycopg2://airflow:airflow@postgres:5432/airflow
AIRFLOW__CORE__SQL_ALCHEMY_CONN=postgresql+psycopg2://airflow:airflow@postgres:5432/airflow
AIRFLOW_CONN_POSTGRES_DEFAULT=postgres://airflow:airflow@postgres:5432/airflow

# --- Celery / Redis ---
AIRFLOW__CELERY__BROKER_URL=redis://redis:6379/0
AIRFLOW__CELERY__RESULT_BACKEND=db+postgresql://airflow:airflow@postgres:5432/airflow
# Délai max pour publier une tâche dans Redis. Défaut = 1s, trop court sur
# un cluster mono-nœud à 2 vCPU (k3s + 10 pods) => "Process timed out"
# suivi du faux coupable "module 'redis' has no attribute 'client'"
# (import interrompu par le signal de timeout).
AIRFLOW__CELERY__OPERATION_TIMEOUT=30
# --- Webserver ---
AIRFLOW__WEBSERVER__ENABLE_PROXY_FIX=true
AIRFLOW__WEBSERVER__BASE_URL=https://52.19.146.187/airflow
AIRFLOW__WEBSERVER__RATELIMIT_STORAGE_URI=redis://redis:6379/0

# --- Webserver ---
# Via le Makefile, ces 2 variables sont mises dans le .env.secret avec la valeur affécté
# _AIRFLOW_WWW_USER_USERNAME: "${_AIRFLOW_WWW_USER_USERNAME:-admin}"
# _AIRFLOW_WWW_USER_PASSWORD: "${_AIRFLOW_WWW_USER_PASSWORD:-admin}"

#AIRFLOW__LOGGING__LOGGING_CONFIG_CLASS=log_config.DEFAULT_LOGGING_CONFIG
#AIRFLOW_HOME=/app
# Pour être MLOPs pro, on utilise le dir naturel de l'image airflow
AIRFLOW_HOME=/opt/airflow
# On force aussi l'emplacement du fichier de configuration
#AIRFLOW_CONFIG=/app/airflow.cfg
#AIRFLOW__LOGGING__BASE_LOG_FOLDER=/app/logs
AIRFLOW_CONFIG=/opt/airflow/airflow.cfg
AIRFLOW__LOGGING__BASE_LOG_FOLDER=/opt/airflow/logs

# --- API ---
AIRFLOW__API__AUTH_BACKENDS=airflow.api.auth.backend.basic_auth,airflow.api.auth.backend.session

# --- DVC ---
DVC_REMOTE=origin

# --- DAGSHUG ---
# DAGSHUB_REPO_NAME: ${DAGSHUB_REPO_NAME}
DAGSHUB_REPO_NAME=Template_MLOps_accidents
AWS_ENDPOINT_URL=https://dagshub.com/s3

# --- MLFLOW DU DAGSHUB ---
# Via le Makefile, ces 2 variables sont mises dans le .env.secret avec la valeur affécté
# MLFLOW_TRACKING_USERNAME: ${DAGSHUB_USER}
# MLFLOW_TRACKING_PASSWORD: ${DAGSHUB_S3_SECRET_ACCESS_KEY}
